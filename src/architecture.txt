
'UNIFIED'

DATA:
    D1: UI Elements
    D2: Theme data (name, customizations... )
    D3: Configuration data (workbenchColorCustomizations...)


ELEMENTS (Univeral)

    Standard-view Group

        ICON (any):
            simple folder/group icon -> always

        LABEL:(string)
            elementData['group'] -> always

        DESCRIPTION:
            None -> always

        TOOLTIP:
            Element info -> always

    Standard-view Item

        ICON (any):

            Dot -> Theme/Default
            Dripping dot -> Customized

        LABEL:(string)
            elementData['groupedName'] -> always

        DESCRIPTION:
            color -> always

        TOOLTIP:
            Element info -> always

    Palette-view Group

        ICON (any):

            Simple folder/group icon

        LABEL:(string)
            Group <x> -> always

        DESCRIPTION:
            color -> always

        TOOLTIP:
            None

    Palette-view Item

        ICON (any):
            Dot -> Theme/Default
            Dripping dot -> Customized

        LABEL:(string)
            elementData["titleName"]

        DESCRIPTION:
            *None

        TOOLTIP:
            Element info -> always


VIEWS:

    - Menu/info
    - Standard view
    - Palette view

For 1.0:

    - No filter, effective color always shows, with others retrievable via context command

        AKA if you want to make a theme, clean ur shit ass settings up first ya goof, if you can't be bothered to do that then fuck ya'



FUNCTION:

    getTreeItem(ElementData) Element

        Steps:

            1: Check

CLASSES:



    (
        label
        collapsibleState
        defaultColor?
        themeColor?
        customizationColor?
    )

